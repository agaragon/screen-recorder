<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Chunks written to external cache, shareable via FileProvider -->
    <external-cache-path name="video_chunks" path="." />
    <!-- Default Movies directory for recordings saved without SAF -->
    <external-path name="movies" path="Movies/" />
</paths>
